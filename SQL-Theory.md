# SQL-Theory

## 1.	Что такое SQL?
SQL (Structured Query Language) — это язык запросов, используемый для управления и работы с реляционными базами данных. SQL позволяет создавать, изменять и извлекать данные из базы данных, а также управлять доступом и обеспечивать целостность данных.
### Основные функции SQL:
#### Создание и изменение структуры данных:
DDL (Data Definition Language): Команды для создания и изменения структуры таблиц и других объектов базы данных.

    📌Пример:
    CREATE TABLE users (
      id INT PRIMARY KEY,
      name VARCHAR(100),
      age INT
    );
    ALTER TABLE users ADD email VARCHAR(100);
    DROP TABLE users;
#### Манипулирование данными:
DML (Data Manipulation Language): Команды для вставки, обновления, удаления и извлечения данных из таблиц.

    📌Пример:
    INSERT INTO users (id, name, age) VALUES (1, 'John Doe', 30);
    UPDATE users SET age = 31 WHERE id = 1;
    DELETE FROM users WHERE id = 1;
    SELECT * FROM users;
#### Управление доступом:
DCL (Data Control Language): Команды для управления правами доступа пользователей к данным.

    📌Пример:
    GRANT SELECT, INSERT ON users TO 'username';
    REVOKE SELECT ON users FROM 'username';
#### Обеспечение целостности данных:
TCL (Transaction Control Language): Команды для управления транзакциями, которые позволяют объединять несколько операций в одну, обеспечивая их атомарность.

    📌Пример:
    BEGIN TRANSACTION;
    UPDATE accounts SET balance = balance - 100 WHERE account_id = 1;
    UPDATE accounts SET balance = balance + 100 WHERE account_id = 2;
    COMMIT;
    ROLLBACK;
### Основные команды SQL:
* SELECT: Извлечение данных из таблиц.

      SELECT name, age FROM users;
* INSERT: Вставка новых данных в таблицы.

      INSERT INTO users (id, name, age) VALUES (1, 'John Doe', 30);
* UPDATE: Обновление существующих данных.

      UPDATE users SET age = 31 WHERE id = 1;
* DELETE: Удаление данных из таблиц.

      DELETE FROM users WHERE id = 1;
* CREATE: Создание новых таблиц и других объектов базы данных.

      CREATE TABLE users (
        id INT PRIMARY KEY,
        name VARCHAR(100),
        age INT
      );
* ALTER: Изменение структуры существующих таблиц.

      ALTER TABLE users ADD email VARCHAR(100);
* DROP: Удаление таблиц и других объектов базы данных.

      DROP TABLE users;
## 2.	Что такое БД?
База данных (БД) — это организованная коллекция данных, которая систематически хранится и управляется таким образом, чтобы к ней легко можно было получить доступ, обновить и управлять ею. Базы данных используются для хранения информации в структурах, которые обеспечивают эффективный поиск и манипулирование данными.
### Основные характеристики базы данных:
#### Организованность:
* Данные в базе данных структурированы в таблицы, которые состоят из строк и столбцов.
* Таблицы могут быть связаны друг с другом с помощью ключей (первичных и внешних).
#### Хранилище данных:
* Базы данных используются для хранения больших объемов данных, включая текстовую информацию, изображения, видео и др.
#### Доступ к данным:
* Базы данных обеспечивают быстрый и эффективный доступ к данным с помощью языков запросов, таких как SQL.

## 3.	Какие бываю БД?
### Реляционные базы данных (RDBMS):
* Принцип: Основаны на реляционной модели данных, где данные хранятся в таблицах, состоящих из строк и столбцов.
* 📌Примеры: MySQL, PostgreSQL, Oracle, Microsoft SQL Server.
* Использование: Банковские системы, онлайн-магазины, системы управления транзакциями.
### Документные базы данных (NoSQL):
* Принцип: Хранят данные в виде документов, обычно в формате JSON, BSON или XML.
* 📌Примеры: MongoDB, CouchDB.
* Использование: Управление контентом, хранение конфигураций, веб-приложения.
### Графовые базы данных:
* Принцип: Хранят данные в виде графов, где узлы представляют сущности, а ребра — связи между ними.
* 📌Примеры: Neo4j, OrientDB.
* Использование: Социальные сети, системы рекомендаций, анализ отношений.
### Колонночные базы данных (NoSQL):
* Принцип: Хранят данные по столбцам, а не по строкам, что улучшает производительность при больших объемах данных и аналитических запросах.
* 📌Примеры: Apache Cassandra, HBase.
* Использование: Аналитические системы, большие данные, высоконагруженные сервисы.
### Ключ-значение базы данных (NoSQL):
* Принцип: Хранят данные в виде пар "ключ-значение". Очень быстры для чтения и записи данных.
* 📌Примеры: Redis, Riak, DynamoDB.
* Использование: Кэширование, управление сессиями, быстрый доступ к данным.
### Временные базы данных (Time-series DB):
* Принцип: Оптимизированы для хранения и обработки временных данных, таких как метрики и лог-файлы.
* 📌Примеры: InfluxDB, TimescaleDB.
* Использование: Мониторинг производительности, IoT, аналитика временных рядов.
### Объектно-ориентированные базы данных (OODBMS):
* Принцип: Хранят данные в виде объектов, как и в объектно-ориентированном программировании.
* 📌Примеры: ObjectDB, db4o.
* Использование: Системы, где удобнее работать с объектами, интеграция с объектно-ориентированными языками программирования.
### Многомерные базы данных (OLAP DB):
* Принцип: Хранят данные в многомерных структурах, часто используемых в OLAP для аналитики.
* 📌Примеры: SAP BW, Microsoft Analysis Services.
* Использование: Бизнес-аналитика, построение отчетов, анализ больших данных.
### Гибридные базы данных (Multi-model DB):
* Принцип: Объединяют принципы и преимущества различных типов баз данных.
* 📌Примеры: ArangoDB (графовая и документная), Cosmos DB (поддерживает несколько моделей данных).
* Использование: Универсальные системы, требующие гибкости в управлении различными типами данных.

## 4.	В каком виде хранятся данные в Реляционных БД?
В реляционных базах данных (RDBMS) данные хранятся в виде таблиц. Эти таблицы состоят из строк и столбцов, а структура данных описывается схемой базы данных. Вот как это работает в деталях:
### Структура данных в реляционных БД:
#### Таблицы:
* Основные структуры для хранения данных. Каждая таблица состоит из строк (записей) и столбцов (полей).
* Таблицы могут быть связаны между собой с помощью ключей.
### Основные компоненты таблицы:
#### Столбцы (Columns):
* Определяют тип данных, которые будут храниться в таблице.
* Каждый столбец имеет имя и тип данных (например, INTEGER, VARCHAR, DATE).

        📌Пример:
        CREATE TABLE employees (
          id INT PRIMARY KEY,
          name VARCHAR(100),
          position VARCHAR(50),
          salary DECIMAL(10, 2)
        );
#### Строки (Rows):
* Каждая строка представляет собой одну запись в таблице и содержит данные для каждого столбца.

        📌Пример:
        id | name          | position      | salary
        -------------------------------------------
        1  | John Doe      | Manager       | 50000.00
        2  | Jane Smith    | Developer     | 45000.00
### Связи между таблицами:
#### Первичный ключ (Primary Key):
* Уникальный идентификатор записи в таблице.
* Каждая таблица должна иметь первичный ключ, который гарантирует уникальность записей.

        📌Пример:
        CREATE TABLE departments (
          id INT PRIMARY KEY,
          name VARCHAR(100)
        );
#### Внешний ключ (Foreign Key):
* Связывает строки в одной таблице с записями в другой таблице.
* Внешний ключ устанавливает связь между таблицами для поддержания целостности данных.

        📌Пример:
        CREATE TABLE employees (
          id INT PRIMARY KEY,
          name VARCHAR(100),
          department_id INT,
          FOREIGN KEY 

## 5.	Что такое СУБД
СУБД (система управления базами данных) — это программное обеспечение, которое обеспечивает создание, управление, модификацию и контроль баз данных. СУБД предоставляет пользователям и приложениям инструменты для работы с данными, обеспечивая доступ, управление и безопасность данных.
### Основные функции СУБД:
#### Создание базы данных:
* СУБД предоставляет средства для создания структуры базы данных, включая таблицы, индексы и схемы.
* 📌Пример: Создание таблицы CREATE TABLE.
#### Управление данными:
* СУБД обеспечивает вставку, обновление, удаление и выборку данных.
* 📌Примеры: Команды SQL INSERT, UPDATE, DELETE, SELECT.
#### Обеспечение целостности данных:
* Поддержка правил целостности данных для предотвращения ошибок и дублирования.
* 📌Пример: Ограничения первичного и внешнего ключа.
#### Управление транзакциями:
* Обеспечение атомарности, согласованности, изолированности и долговечности (ACID) операций.
* 📌Примеры: Команды BEGIN, COMMIT, ROLLBACK.
#### Безопасность данных:
* Контроль доступа пользователей и защита данных от несанкционированного доступа.
* 📌Примеры: Команды GRANT, REVOKE.
#### Резервное копирование и восстановление:
* Обеспечение защиты данных путем создания резервных копий и восстановления данных в случае сбоев.
* 📌Пример: Инструменты резервного копирования.
#### Оптимизация запросов:
* СУБД оптимизирует выполнение запросов для повышения производительности.
* 📌Пример: Использование индексов для ускорения поиска данных.
### Типы СУБД:
#### Реляционные СУБД (RDBMS):
* Основаны на реляционной модели данных.
* 📌Примеры: MySQL, PostgreSQL, Oracle, Microsoft SQL Server.
#### Нереляционные СУБД (NoSQL):
* Предназначены для работы с неструктурированными данными.
* 📌Примеры: MongoDB, Cassandra, Redis.
#### Объектно-ориентированные СУБД:
* Хранят данные в виде объектов, как и в объектно-ориентированном программировании.
* 📌Примеры: ObjectDB, db4o.
#### Графовые СУБД:
* Хранят данные в виде графов, представляющих узлы и связи.
* 📌Примеры: Neo4j, OrientDB.
### Примеры использования СУБД:
#### Банковские системы:
* Управление счетами, транзакциями и клиентскими данными.
* 📌Пример: Реляционные СУБД для обеспечения целостности данных.
#### Интернет-магазины:
* Хранение информации о товарах, заказах и клиентах.
* 📌Пример: NoSQL для работы с большими объемами неструктурированных данных.
#### Социальные сети:
* Хранение информации о пользователях, их постах, друзьях и сообщениях.
* 📌Пример: Графовые СУБД для управления сложными связями между пользователями.

## 6.	Какие ты знаешь СУБД?
### MySQL
Считается одной из самых распространенных СУБД. MySQL — реляционная СУБД с открытым исходным кодом, главными плюсами которой являются ее скорость и гибкость, которая обеспечена поддержкой большого количества различных типов таблиц.
Кроме того, это надежная бесплатная система с простым интерфейсом и возможностью синхронизации с другими базами данных. В совокупности эти факторы позволяют использовать MySQL как крупным корпорациям, так и небольшим компаниям.
### Microsoft SQL Server
Как следует из названия, фирменная СУБД, разработанная Microsoft. Оптимальная для использования в операционных системах семейства Windows, однако может работать и с Linux.
Система позволяет синхронизироваться с другими программными продуктами компании Microsoft, а также обеспечивает надежную защиту данных и простой интерфейс, однако отличается высокой стоимостью лицензии и повышенным потреблением ресурсов.
В целом, однако, сохраняет свою популярность, в немалой степени из-за того, что продукты корпорации Microsoft используются многими компаниями.
### PostgreSQL
СУБД PostgreSQL — еще одна популярная и бесплатная система. Наибольшее применение нашла для управления БД веб-сайтов и различных сервисов. Она универсальна, то есть подойдет для работы с большинством популярных платформ.
При этом PostgreSQL — объектно-реляционная СУБД, что дает ей некоторые преимущества над другими бесплатными СУБД, в большинстве являющимися реляционными.
### Oracle
Первая версия этой объектно-реляционной СУБД появилась в конце 70-х, и с тех пор зарекомендовала себя как надежная, функциональная и практичная. СУБД Oracle постоянно развивается и дорабатывается, упрощая установку и первоначальную настройку и расширяя функционал.
Однако существенным минусом данной СУБД является высокая стоимость лицензии, поэтому она используется в основном крупными компаниями и корпорациями, работающими с огромными объемами данных.

## 7.	Какая разница между MySQL и SQL?
### SQL (Structured Query Language)
SQL — это язык запросов, используемый для работы с реляционными базами данных. Он обеспечивает возможность создания, манипулирования и управления данными в базе данных.
#### Основные особенности SQL:
* Язык запросов: Позволяет выполнять команды для создания, изменения, удаления и выборки данных.
* Стандарты: SQL — это стандарт, который поддерживается большинством реляционных СУБД.
### MySQL
MySQL — это система управления базами данных (СУБД), которая использует SQL для работы с данными. MySQL — одна из наиболее популярных и широко используемых реляционных СУБД.
#### Основные особенности MySQL:
* СУБД: MySQL — это конкретная реализация СУБД, которая использует SQL для управления данными.
* Открытый исходный код: MySQL является бесплатной и имеет открытую лицензию, что делает её популярной для использования в различных проектах.
* Производительность: Оптимизирована для высокой производительности и быстрой обработки запросов.
* Расширяемость: Поддерживает различные хранилища и плагины, что позволяет адаптировать её под различные нужды.
* 📌Примеры использования: Веб-приложения, системы управления контентом (CMS), онлайн-магазины и т.д.
### Основные различия между SQL и MySQL:
#### SQL:
* Это язык запросов, используемый для взаимодействия с базами данных.
* Независим от конкретной СУБД и является стандартом для большинства реляционных баз данных.
#### MySQL:
* Это конкретная СУБД, которая использует SQL для управления данными.
* Является одним из множества СУБД, которые поддерживают SQL (например, PostgreSQL, Oracle, Microsoft SQL Server).

## 8.	Что такое первичный ключ (Primary key)?
Первичный ключ (Primary Key) — это уникальный идентификатор строки в таблице реляционной базы данных. Он гарантирует, что каждая запись в таблице уникальна и может быть однозначно идентифицирована.
### Основные характеристики первичного ключа:
#### Уникальность:
Первичный ключ гарантирует, что каждое значение в этом столбце уникально. В таблице не может быть двух строк с одинаковым значением первичного ключа.
#### Не допускает NULL значений:
Значение первичного ключа не может быть NULL. Это означает, что каждая строка должна иметь значение в столбце первичного ключа.
#### Одна таблица — один первичный ключ:
В таблице может быть только один первичный ключ, хотя он может состоять из одного или нескольких столбцов (составной ключ).
#### 📌Примеры использования первичного ключа:
##### Простой первичный ключ:
Один столбец используется в качестве уникального идентификатора.

    CREATE TABLE users (
      id INT PRIMARY KEY,
      name VARCHAR(100),
      email VARCHAR(100)
    );
##### Составной первичный ключ:
Несколько столбцов вместе образуют уникальный идентификатор.

    CREATE TABLE orders (
      order_id INT,
      product_id INT,
      PRIMARY KEY (order_id, product_id)
    );
### Зачем нужен первичный ключ:
#### Идентификация записей:
Уникально идентифицирует каждую запись в таблице, что облегчает поиск, обновление и удаление данных.
#### Обеспечение целостности данных:
Гарантирует, что каждая запись уникальна и нет дублирующихся данных.
#### Связи между таблицами:
Используется для создания связей между таблицами через внешние ключи (FOREIGN KEY).

## 9.	Что такое внешний ключ (Foreign key)?
Внешний ключ (Foreign Key) — это столбец или набор столбцов в одной таблице, который указывает на первичный ключ в другой таблице. Внешний ключ используется для установления и обеспечения ссылочной целостности между двумя таблицами, связывая их на уровне данных.
### Основные характеристики внешнего ключа:
#### Ссылочная целостность:
Внешний ключ гарантирует, что значения в одной таблице соответствуют значениям в другой таблице.
Он обеспечивает целостность данных, гарантируя, что каждая запись в дочерней таблице имеет соответствующую запись в родительской таблице.
#### Связи между таблицами:
Внешний ключ устанавливает отношения между таблицами, такие как "один ко многим" и "многие ко многим".

### 📌Пример внешнего ключа:
#### Создание таблиц с внешним ключом:
В этом примере мы создаем таблицу departments с первичным ключом и таблицу employees, которая имеет внешний ключ, указывающий на departments.

    CREATE TABLE departments (
      department_id INT PRIMARY KEY,
      department_name VARCHAR(50)
    );
    
    CREATE TABLE employees (
      employee_id INT PRIMARY KEY,
      first_name VARCHAR(50),
      last_name VARCHAR(50),
      department_id INT,
      FOREIGN KEY (department_id) REFERENCES departments(department_id)
    );
###📌Пример внешнего ключа:
#### Создание таблиц с внешним ключом:
В этом примере мы создаем таблицу departments с первичным ключом и таблицу employees, которая имеет внешний ключ, указывающий на departments.

    CREATE TABLE departments (
      department_id INT PRIMARY KEY,
      department_name VARCHAR(50)
    );
    
    CREATE TABLE employees (
      employee_id INT PRIMARY KEY,
      first_name VARCHAR(50),
      last_name VARCHAR(50),
      department_id INT,
      FOREIGN KEY (department_id) REFERENCES departments(department_id)
    );
#### Добавление внешнего ключа в существующую таблицу:
Если таблица уже существует, можно добавить внешний ключ с помощью команды ALTER TABLE.

    ALTER TABLE employees
    ADD CONSTRAINT fk_department
    FOREIGN KEY (department_id)
    REFERENCES departments(department_id);

###📌Примеры использования внешнего ключа:
#### Связь "один ко многим":
* Внешний ключ в дочерней таблице ссылается на первичный ключ в родительской таблице.
* 📌Пример: Один департамент (родительская таблица) может иметь много сотрудников (дочерняя таблица).
#### Связь "многие ко многим":
* Требует создания промежуточной таблицы с двумя внешними ключами.
* 📌Пример: Таблицы students, courses и промежуточная таблица enrollments для хранения информации о том, какие студенты записаны на какие курсы.

        CREATE TABLE students (
          student_id INT PRIMARY KEY,
          student_name VARCHAR(50)
        );
        
        CREATE TABLE courses (
          course_id INT PRIMARY KEY,
          course_name VARCHAR(50)
        );
        
        CREATE TABLE enrollments (
          student_id INT,
          course_id INT,
          PRIMARY KEY (student_id, course_id),
          FOREIGN KEY (student_id) REFERENCES students(student_id),
          FOREIGN KEY (course_id) REFERENCES courses(course_id)
        );
### Зачем нужен внешний ключ:
* Обеспечение целостности данных:
Внешний ключ гарантирует, что данные остаются согласованными между связанными таблицами. Например, нельзя добавить запись в дочернюю таблицу, если соответствующая запись отсутствует в родительской таблице.
* Поддержание связей:
Внешний ключ позволяет управлять и поддерживать связи между различными таблицами в базе данных, облегчая навигацию и работу с данными.
* Упрощение выполнения запросов:
Внешний ключ позволяет легко выполнять запросы, которые объединяют данные из связанных таблиц.

## 10.	Что такое реляции?
Реляции — это концепция реляционных баз данных, где данные организованы и управляются в виде таблиц. Реляции основываются на математической теории отношений (relations), разработанной Эдгаром Коддом. Давай разберем это более подробно.
### Что такое реляция?
Реляция: В реляционных базах данных реляция представляет собой таблицу. Таблица состоит из строк и столбцов, где каждая строка (запись) соответствует конкретному элементу данных, а каждый столбец (поле) представляет атрибут этих данных.
### Основные элементы реляций:
#### Таблица:
* Структура данных, которая организована в виде строк и столбцов.
* 📌Пример:

        Таблица "students":
        id | name  | age | major
        ---|-------|-----|---------
        1  | John  | 21  | Physics
        2  | Alice | 22  | Math
#### Столбец (Атрибут):
* Представляет собой свойства или характеристики данных, хранящихся в таблице.
* 📌Пример: В таблице "students" столбцы — это id, name, age, major.
#### Строка (Тупл):
* Каждая строка в таблице представляет собой отдельный элемент данных, известный как тупл (tuple).
* 📌Пример: Строка (1, 'John', 21, 'Physics') в таблице "students".
#### Домены:
* Определяют возможные значения для каждого атрибута.
* 📌Пример: Для атрибута age домен может быть ограничен положительными целыми числами.
### Связи (отношения) между реляциями:
* Реляционные базы данных часто используют связи между таблицами для организации данных. Эти связи позволяют объединять данные из разных таблиц на основе общего атрибута.
#### Связь "один к одному" (1:1):
* Каждой записи в первой таблице соответствует одна запись во второй таблице.
* 📌Пример: Таблица person и таблица passport, где у каждого человека только один паспорт и у каждого паспорта только один владелец.
#### Связь "один ко многим" (1:N):
* Каждой записи в первой таблице соответствует несколько записей во второй таблице.
* 📌Пример: Таблица department и таблица employees, где каждый департамент может иметь много сотрудников.
#### Связь "многие ко многим" (M:N):
* Каждой записи в первой таблице может соответствовать несколько записей во второй таблице, и наоборот.
* 📌Пример: Таблица students и таблица courses, где каждый студент может быть записан на несколько курсов, и каждый курс может включать нескольких студентов. Для реализации такой связи часто используется промежуточная таблица.

      CREATE TABLE student_course (
      student_id INT,
      course_id INT,
      PRIMARY KEY (student_id, course_id),
      FOREIGN KEY (student_id) REFERENCES students(id),
      FOREIGN KEY (course_id) REFERENCES courses(id)
        );

### 📌Примеры SQL-запросов для работы с реляциями:
* Создание таблицы:

      CREATE TABLE students (
      id INT PRIMARY KEY,
      name VARCHAR(100),
      age INT,
      major VARCHAR(100)
      );
* Добавление данных в таблицу:

      INSERT INTO students (id, name, age, major) VALUES (1, 'John', 21, 'Physics');
      INSERT INTO students (id, name, age, major) VALUES (2, 'Alice', 22, 'Math');
* Извлечение данных из таблицы:

      SELECT * FROM students WHERE major = 'Physics';
* Объединение данных из нескольких таблиц:

      SELECT students.name, courses.course_name
      FROM students
      JOIN student_course ON students.id = student_course.student_id
      JOIN courses ON student_course.course_id = courses.id;

## 11.	зачем нужны реляции? приведи пример реляции как она вообще работает?
## 12.	Что такое соединения в SQL?
## 13.	Какие ты  знаешь JOIN?
## 14.	Расскажи подробно про FULL JOIN
## 15.	Что такое Union?
## 16.	В чем разница между UNION и UNION ALL ?
## 17.	Что такое SELECT ?
## 18.	Что такое подзапрос?
## 19.	Какая очередность выполнения когда есть подзапрос? что первым выполняется?
## 20.	Какие ограничения существуют при создании таблиц?
## 21.	Что такое Group By
## 22.	Что такое Order by?
## 23.	Что нужно указать что бы было по возрастанию или по убыванию в операторе Order by ?
## 24.	Что такое INSERT?
## 25.	Что такое Update?
## 26.	Какое отличие WHERE и HAVING
## 27.	Какие знаеш агрегатные функции?
## 28.	Для чего используются операторы IN, BETWEEN, LIKE?
## 29.	Какие условия выполнения операторов OR и AND
## 30.	Оператор SQL LIMIT
## 31.	Чем NULL отличается от 0
## 32.	В чем разница между операторами GROUP BY и DISTINCT?
## 33.	Что такое нормализация и каковы ее преимущества?
## 34.	Какие формы нормализации существуют?
## 35.	Что такое денормализация?
## 36.	Из каких подмножеств состоит SQL?
## 37.	Какие существуют типы связей в базе данных? Приведите примеры.
## 38.	Какие ты можешь использовать Числовые типы данных при создании таблици?
## 39.	Какие ты можешь использовать Текстовые типы данных при создании таблици?
## 40.	Какие ты можешь использовать типы данных Даты при создании таблици?
## 41.	В чем разница между типом данных CHAR и VARCHAR в SQL?
## 42.	Что бы работали джоины нужны ли реляции?
